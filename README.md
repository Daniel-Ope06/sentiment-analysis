# Sentiment Analysis

This repository contains a complete Natural Language Processing (NLP) pipeline that tracks the evolution of sentiment analysis. It benchmarks traditional statistical models (BoW, TF-IDF) directly against advanced deep learning architectures (BiLSTM, DistilBERT). From custom text preprocessing to deep qualitative error analysis, this project demonstrates exactly why dynamic, contextual AI vastly outperforms older, static methods.
